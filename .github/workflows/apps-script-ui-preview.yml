name: HT-PD-RD UI Preview — FROZEN BY BACKEND P0

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - name: BACKEND GATE P0
        run: |
          echo 'UX/mobile work and UI deployment are frozen until BACKEND_READY has Apps Script UAT runtime evidence.'
          echo 'Canonical runtime source: Hai-hatien/hatien-product-rd backend/v1.3-hardening'
          echo 'Do not deploy UI, do not use a transcription-uncertain Script ID, and do not use another visible Apps Script project.'
          exit 1
