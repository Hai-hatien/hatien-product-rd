import assert from 'node:assert/strict';
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const index = fs.readFileSync(path.join(root, 'apps-script/ht-pd-rd-v1/Index.html'), 'utf8');
const client = fs.readFileSync(path.join(root, 'apps-script/ht-pd-rd-v1/Client.html'), 'utf8');
const forbidden = /\b(dashboard|backend|frontend|publish|lifecycle|uat|checklist|status|task|blocker|pass|fail|golive|review|approval|workflow|queue|evidence|claim|gate)\b/i;
const visibleText = index.replace(/<script[\s\S]*?<\/script>/gi, '').replace(/<style[\s\S]*?<\/style>/gi, '').replace(/<[^>]+>/g, ' ').replace(/&amp;/g, '&');
assert.match(client, /const LANGUAGE_DICTIONARY/);
assert.match(client, /let languageMode = 'vi'/);
assert.match(index, /id="languageToggle"/);
assert.doesNotMatch(visibleText, forbidden, 'Vietnamese static UI contains a forbidden technical term');
assert.match(client, /function toggleLanguage/);
assert.match(client, /function proposalSection/);
assert.match(client, /function missingProposalFields/);
assert.match(client, /TIẾP TỤC LÀM NGAY/);
for (const heading of ['Đề xuất xử lý', 'Vì sao đáng làm', 'Ai bị ảnh hưởng', 'Cơ hội cho Hà Tiên', 'Bằng chứng đang có', 'Rủi ro cần chặn', 'Việc cần anh Hải quyết', 'Bước tiếp theo nếu đồng ý', 'Kết luận đề xuất', 'Vấn đề thị trường', 'Người đọc chính', 'Nỗi đau và quyết định của khách', 'Góc bài đề xuất', 'Cấu trúc bài nếu được duyệt', 'Xem hồ sơ đề xuất']) assert.match(client, new RegExp(heading));
assert.match(client, /function openArticleProposal/);
assert.match(client, /function missingArticleProposalFields/);
assert.match(index, /id="htmlPreviewModal"/);
for (const tab of ['Hiển thị', 'Mã fragment', 'Dữ liệu web', 'Kiểm contract']) assert.match(index, new RegExp(tab));
for (const fn of ['approveArticleProposal', 'approveArticleContent', 'openArticleHtmlPreview', 'confirmHtmlPreview', 'sendArticleDraft']) assert.match(client, new RegExp(`function ${fn}`));
assert.match(client, /status: 'draft'/);
assert.match(client, /HATIENCORP_WEB_ARTICLE_STANDARD_v2\.1/);
assert.match(client, /send\.disabled = !\(state\.proposalApproved && state\.contentApproved && state\.previewOpened && state\.htmlConfirmed\)/);
assert.doesNotMatch(client, /function sendArticleDraft[\s\S]*?google\.script\.run/);
const cardTemplate = client.match(/return `<article class="decision-card">[\s\S]*?`;/)?.[0] || '';
assert.doesNotMatch(cardTemplate, /ACT_NOW_CONTINUE|School Kitchen Readiness\/Audit/);
assert.match(client, /function userText/);
assert.match(client, /google\.script\.run/);
console.log('language-contract: PASS');
